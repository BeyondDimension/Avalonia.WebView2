#if !(WINDOWS || NETFRAMEWORK) && NET8_0_OR_GREATER && !ANDROID && !IOS && !MACOS && !MACCATALYST && DISABLE_CEFGLUE && !DISABLE_CEFNET

namespace Avalonia.Controls;

partial class WebView2
{
}
#endif